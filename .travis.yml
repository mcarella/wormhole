language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wormhole' ]; then cd .. && eval "mv $currentfolder generator-wormhole" && cd generator-wormhole; fi

